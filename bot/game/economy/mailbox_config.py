"""
REMOVED -- the mailbox config is superseded by the Research Lab and the
Forge (bot/game/economy/research_config.py, forge_config.py and their
services).

The mailbox was a building you waited on and then collected from, which
is exactly what harvesters already are: two systems in one design slot,
so players correctly ignored the weaker one. It was replaced rather than
rebalanced, because a bigger number on a redundant system is still a
redundant system.

This file is intentionally empty and can be deleted.
"""
